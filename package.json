{
  "name": "react-modal-oc",
  "version": "1.1.1",
  "description": "Basic Modal in react",
  "main": "lib/index.js",
  "keywords": [
    "modal",
    "react"
  ],
  "scripts": {
    "build": "babel src -d lib",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "matohoundev@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react": "^6.24.1"
  }
}
